apiVersion: v1
kind: Service
metadata:
  name: vllm-service
  namespace: llm-demo
  labels:
    app: vllm-qwen25-7b
    release: monitoring
spec:
  type: ClusterIP
  selector:
    app: vllm-qwen25-7b
  ports:
    - name: http
      port: 80
      targetPort: 8000
      protocol: TCP
