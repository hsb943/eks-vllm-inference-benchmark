apiVersion: v1
kind: Service
metadata:
  name: vllm-service
  namespace: llm-demo
spec:
  type: ClusterIP
  selector:
    app: vllm-llama31-8b
  ports:
    - name: http
      port: 80
      targetPort: 8000
      protocol: TCP

